numbers = [1, 2, 3, 4]

# A list is an iterable. Use a for loop to go through each element
for x in numbers:
	print(x)

string = "hello, world"

# Strings are iterable too!
for c in string:
	print(c)
